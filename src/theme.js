import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#FF5A5F',
    },
    secondary: {
      main: '#00A699', 
    },
        background: {
      default: '#FF5A5F', 
    },
  },
});

export default theme;
